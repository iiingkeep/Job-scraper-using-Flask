# Method: 데이터 뒤에 결합/연결된 function으로, 단독 사용 불가. 앞에 데이터와 꼭 연결지어 사용, 데이터를 변환/조작한다.
# 데이터 타입에 따라 다른 method들이 존재한다.
# 형식: data.method(options)

name = 'Mary'
print(name.upper())
print(name.startswith('j'))
